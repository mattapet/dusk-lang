extern func test();
extern func println(output: Int) -> Void;

func main(arg1: Int) -> Int {
    for i in 0..arg1 {
        println(i);
        if i > 5 {
            break;
        }
        break;
    }
    return 0;
}

func inc(i: Int) -> Int {
    return i + 1;
}
