extern func test();
extern func println(output: Int) -> Void;

func main(arg1: Int) -> Int {
    if arg1 % 2 == 0 {
        return inc(arg1);
    }
    println(arg1);
    return arg1;
}

func inc(i: Int) -> Int {
    return i + 1;
}
