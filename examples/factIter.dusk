func factor(n: Int) -> Int {
    var f = 1;
    var n = n;

    while n >= 2 {
        f = f * n;
        n = n - 1;
    }
    return f;
}

func main() {
    println(factor(readln()));
}

/*
program factorialCycle;

var
    n: integer;
    f: integer;
begin
    f := 1;
    readln(n);
    while(n >= 2) do begin
        f := f * n;
	dec(n);
    end;
    writeln(f);
end.
*/
